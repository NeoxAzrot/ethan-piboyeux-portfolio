type ProjectFilterType = {
  year: number;
  services: string;
  link: string;
  image: string;
  class: string;
  filter: string;
};

type ProjectType = {
  name: string;
  year: number;
  services: string;
  link: string;
  image: string;
  alt: string;
};

type CreditsType = {
  name: string;
  role: string;
};
